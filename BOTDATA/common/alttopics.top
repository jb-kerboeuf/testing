
outputmacro: tryalttopics()
  ^query(direct_vo ? isAltTopic blabla)
  $_before = %response
  ^loop(@0) {
    $_altTopic = ^next(FACT @0subject)

    nofail(TOPIC ^respond($_altTopic)) # try the topic
    if (%response > $_before) { ^end(RULE) } # stop when we find something to say
  }
